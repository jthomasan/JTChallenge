import React from 'react';
import DynamicUserDetails from './components/DynamicUserDetails';


//A simple component which is container of all the components in this application.
export default () => (
    <DynamicUserDetails></DynamicUserDetails>
);
